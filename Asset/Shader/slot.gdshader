shader_type spatial;

render_mode unshaded, cull_back, blend_mix, depth_draw_always;

uniform vec4 color = vec4(0.4, 0.0, 0.6, 0.3);

// On suppose que le plane a UVs de 0..1
void fragment() {
    
}
